specVersion: "v1beta"
serviceId: "recipes"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "recipes-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
